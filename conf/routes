# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# Catch all the routes
GET            /$path<.*>        controllers.ProxyController.any(path)
PUT            /$path<.*>        controllers.ProxyController.any(path)
POST           /$path<.*>        controllers.ProxyController.any(path)
DELETE         /$path<.*>        controllers.ProxyController.any(path)
PATCH          /$path<.*>        controllers.ProxyController.any(path)
HEAD           /$path<.*>        controllers.ProxyController.any(path)
OPTIONS        /$path<.*>        controllers.ProxyController.any(path)
